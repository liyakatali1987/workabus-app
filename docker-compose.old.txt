version: '3.9'
services:
  workabusapp:
      tty: true
      build:
        context: .
        dockerfile: Dockerfile.app
      image: "workabusapp"
      ports:
        - 5173:5173
      working_dir: /app
      command: ls -la app/
      volumes:
        - type: bind
          source: ./app/src
          target: /app/src
volumes:
  app:
    external: false